language: python
sudo: false

matrix:
    include:
        - python: "2.7"
          env: VERSION_FILE=version_plone51.cfg

script:
    - virtualenv . && source ./bin/activate && echo "Virtualenv successfully activated"
    - pip install -U setuptools==36.2.1
    - pip install -r requirements.txt
    - ./bin/buildout -c "$VERSION_FILE"
    - ./bin/test

notifications:
    email: false
